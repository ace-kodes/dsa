package basic_java.collectionsBasics;

import java.util.*;

public class Arraylists {
    public static void main(String[] args)
    {
        List<Integer> arrList = new ArrayList<>();
        arrList.add(1);
        arrList.get(0);
        arrList.set(1, 2); // .set(index, element)
        arrList.remove(1);
        arrList.size();
    }
}
